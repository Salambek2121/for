let mass = ['salam', 'muslim', 'djamal', 'antar', 'muhammad', 'adam', 'islam', 'salah', 'mansur', 'ibrohim']
for(let i = 0; i < mass.length;i++){
    if(mass[i][0] === 'a'){
        console.log(mass[i])
    }
}